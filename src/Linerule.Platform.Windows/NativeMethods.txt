// CsWin32 input — symbols imported into Windows.Win32.*.
// Keep aligned with docs/adr/0009-transparency-via-dcomp.md (dcomp direct,
// no LWA_*).
//
// Conventions per CsWin32 PInvoke004:
//   - List the *enum type* (e.g. WINDOW_STYLE), not its individual constants
//     (WS_POPUP). Constants are accessed via the typed enum at the call site.

// Window lifecycle + Win32 message pump (manual — Phase 2 retired the
// DispatcherQueue-driven loop in favor of GetMessage/Dispatch directly).
// Note: CsWin32 substitutes POINT → System.Drawing.Point in signatures
// automatically; IUnknown is implicit in the COM runtime.
CreateWindowExW
DestroyWindow
RegisterClassExW
UnregisterClassW
DefWindowProcW
ShowWindow
GetMessageW
TranslateMessage
DispatchMessageW
PostQuitMessage
PostMessageW
WM_QUIT
WM_APP
WM_TIMER
SetTimer
KillTimer

// Window styling for click-through + transparency
GetWindowLongPtrW
SetWindowLongPtrW
SetWindowPos
WINDOW_LONG_PTR_INDEX
WINDOW_STYLE
WINDOW_EX_STYLE
HWND_TOPMOST
HWND_MESSAGE
SET_WINDOW_POS_FLAGS
SHOW_WINDOW_CMD

// Cursor + monitor
GetCursorPos
MonitorFromPoint
GetMonitorInfoW
MONITOR_FROM_FLAGS
MONITORINFO
GetDpiForWindow

// Hotkey (HOT_KEY_MODIFIERS is hand-defined in Win32/VirtualKey.cs)
RegisterHotKey
UnregisterHotKey
WM_HOTKEY
WM_CLOSE
WM_DESTROY
WM_DPICHANGED
WM_DISPLAYCHANGE

// Long-press auto-repeat: HotkeyRepeater polls GetAsyncKeyState for the
// chord's VKs between repeat fires, using OS-keyboard-repeat ergonomics
// (250 ms initial delay, 50 ms steady interval). MOD_NOREPEAT on the
// registered hotkey suppresses native WM_HOTKEY flooding, so this is
// the only "is the chord still held?" signal.
GetAsyncKeyState

// Non-blocking pump
PeekMessageW
PEEK_MESSAGE_REMOVE_TYPE

// Primary monitor sizing
GetSystemMetrics
SYSTEM_METRICS_INDEX

// Misc
GetModuleHandleW

// Diagnostics: confirm bridge / framework didn't strip our ex-styles or
// inject a child HWND that intercepts input before our WndProc.
EnumChildWindows
GetClassNameW

// Display refresh rate probe (RenderTiming.Probe).
EnumDisplaySettingsW
DEVMODEW
ENUM_DISPLAY_SETTINGS_MODE

// HUD rendering pipeline (ADR-0009 v3): Direct2D + DirectWrite drawing
// into a Windows.UI.Composition.CompositionDrawingSurface via the
// ICompositionDrawingSurfaceInterop COM ABI. Win32 metadata types listed
// below are projected by Microsoft.Windows.CsWin32 into
// Windows.Win32.Graphics.{Direct2D, DirectWrite, Direct3D11, Dxgi}.*.

// Direct2D — surface drawing primitives.
D2D1CreateFactory
ID2D1Factory1
ID2D1Device
ID2D1DeviceContext
ID2D1SolidColorBrush
D2D1_FACTORY_TYPE
D2D1_FACTORY_OPTIONS
D2D1_ROUNDED_RECT
D2D1_DRAW_TEXT_OPTIONS
D2D_RECT_F
D2D_POINT_2F
D2D1_COLOR_F
D2D1_BRUSH_PROPERTIES

// DirectWrite — text format + layout.
DWriteCreateFactory
IDWriteFactory
IDWriteTextFormat
IDWriteTextLayout
DWRITE_FACTORY_TYPE
DWRITE_FONT_WEIGHT
DWRITE_FONT_STYLE
DWRITE_FONT_STRETCH
DWRITE_WORD_WRAPPING
DWRITE_TEXT_ALIGNMENT
DWRITE_PARAGRAPH_ALIGNMENT
DWRITE_TEXT_METRICS

// Direct3D11 + DXGI — ID3D11Device → IDXGIDevice → ID2D1Device chain
// (BgraSupport flag required for D2D interop). The same ID3D11Device is
// the rendering device passed to DCompositionCreateDevice2.
D3D11CreateDevice
ID3D11Device
D3D11_CREATE_DEVICE_FLAG
D3D_DRIVER_TYPE
D3D_FEATURE_LEVEL
D3D11_SDK_VERSION
IDXGIDevice
DXGI_FORMAT
DXGI_ALPHA_MODE

// DirectComposition — Phase 2: WinAppSDK Compositor retired; we drop to
// dcomp.dll directly so the AOT publish path is free of WinRT projection.
// Device chain at boot:
//   ID3D11Device → DCompositionCreateDevice2(IID_IDCompositionDesktopDevice,
//   out device) → device.CreateTargetForHwnd(hwnd, topmost: false)
//   → CreateVisual / CreateSurface → SetRoot / SetContent → Commit.
// Vsync alignment via DwmFlush() on a dedicated tick thread (preferred over
// WaitForCommitCompletion for AOT predictability — the latter requires
// IDCompositionDevice2 inheritance metadata that CsWin32 doesn't always
// surface for inherited methods).
DCompositionCreateDevice2
IDCompositionDevice2
IDCompositionDesktopDevice
IDCompositionTarget
IDCompositionVisual2
IDCompositionSurface
IDCompositionEffectGroup
DwmFlush
